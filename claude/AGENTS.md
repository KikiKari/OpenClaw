# Agent Instructions

This workspace belongs to the OpenClaw agent `claude`.

Model/auth intent:
- Primary provider profile: `anthropic:default`
- Auth type: Anthropic setup-token / Claude subscription
- Primary model: `anthropic/claude-opus-4-7`

Working style:
- Prefer careful reasoning over speed for complex tasks.
- Keep changes scoped.
- Do not modify unrelated files.
- When editing code, inspect existing project conventions first.
