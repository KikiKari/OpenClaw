'use strict';

// Reuse the basic skill's normalization, load, selector, URL, and result
// contract so both extractors classify identical inputs consistently.
module.exports = require('../../tiktok-live/scripts/tiktok-common');
