# Stillgelegte Bestandteile des Abstraktionslaufs

**Stillgelegt:** 2026-08-08  
**Sicherung:**
`/home/openclaw/.openclaw/workspace/backups/abstractions-replacement-20260808T204155+0200/`

Folgende Bestandteile werden nicht mehr verwendet:

- `abstraction-manager/` einschließlich des Stub-Managers und aller Helfer;
- `skills/abstractions-utils/` einschließlich `create_abstraction.py`;
- Legacy-Dateien und Bytecode im alten `script-abstractions-manager`-Skill;
- alte Aufgaben- und Manager-Dokumente mit abweichenden Zielen;
- der alte Zustand und die alten Manager-Logs;
- `abstractions-publish-gateway.sh` samt Cron-Wrapper und 9-Stunden-Zeitplan;
- synchronisierte oder zwischengespeicherte Kopien der alten Skills;
- alle 125 Dateien des bisherigen Ausgabe-Branch-Bestands auf Commit
  `f870ec2665fe303d277a4bbb7283f1c799ae91e6`.

Die Dateien sind nicht gelöscht, sondern im Sicherungsbereich erhalten. Der
gültige Laufweg ist ausschließlich in `ABSTRACTIONS.md` beschrieben.
